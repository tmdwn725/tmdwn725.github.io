# 블로그 작성 규칙

Chirpy 기반 개인 기술 블로그. 새 글을 쓰거나 기존 글을 고칠 때 이 규칙을 따른다.

## 이 블로그의 정체성

**LLM 에이전트 플랫폼을 만드는 사람의 기록.** 에이전트 하나가 아니라 여러 조직이 각자의
에이전트를 올려 쓰는 밑단을 만든다. 온프렘 추론(DGX)은 그중 일부이지 전부가 아니다.

글의 기준: **잘 된 결과보다 왜 안 됐는지를 쓴다.** 튜토리얼이 아니라 실제로 막혔던 지점과
그걸 어떻게 뚫었는지가 내용이다.

## 카테고리 (정확히 2단계, 아래 목록에서만 고른다)

이 트리는 "나는 AI Platform Engineer 다"라는 주장의 목차다.
About 의 "다루는 영역" 과 짝을 이룬다. 한쪽만 바꾸지 않는다.

| 1단계 | 2단계 | 무엇 |
|---|---|---|
| Agent Platform | Runtime | 도구 호출 루프, 세션·메모리, human-in-the-loop |
| Agent Platform | Gateway | 프로바이더 추상화, 키·예산·사용량 집계 |
| Agent Platform | Integration | MCP, 외부 커넥터 OAuth, SDK, 도구 등록 규약 |
| Agent Platform | Guardrails | 민감정보 필터링, 도구 권한, 외부 접근 제어, 승인 게이트 |
| Infrastructure | Inference | vLLM, GPU, 모델 서빙, 대역폭·메모리 |
| Infrastructure | Deployment | 컨테이너, CI/CD, 리버스 프록시, 운영 |
| Infrastructure | Security | 침해 대응, 접근 통제, 인증 인프라 |
| Infrastructure | Performance | 부하 테스트, 프로파일링, 병목 추적 |
| Engineering | Backend | Java·Spring, DB, API. AI 이전부터 쌓인 층 |
| Engineering | Architecture | 스키마·API 설계 결정과 그 근거 |
| Engineering | Retrospective | 회고, 판단 착오, 되돌아본 선택 |
| Notes | Book | 책을 읽고 내가 바꾼 것 |
| Notes | Talk | 강연·영상을 보고 내가 바꾼 것 |
| Notes | Paper | 논문을 읽고 내가 바꾼 것 |

세 부모의 역할:

```
Agent Platform   모델 위에 얹히는 층       가장 두꺼워야 한다
Infrastructure   모델 아래 기계와 운영
Engineering      그 밖의 엔지니어링 craft  가장 약한 부모. 자식 추가를 경계한다
```

- **1단계는 절대 새로 만들지 않는다.** 4개가 전부다.
- 2단계를 추가해야 하면 **이 표를 먼저 고치고** 글을 쓴다. 표에 없는 값을 쓰지 않는다.
- **`Notes` 는 요약글 창구가 아니다.** Notes 글은 마지막에 **"그래서 내가 바꾼 것"**
  섹션이 반드시 있어야 한다. 없으면 발행하지 않는다. 개념 설명이 필요하면
  **내 환경의 실측값으로 설명한다.** 남의 예시를 옮기지 않는다.
- 한 글은 카테고리 하나만 가진다. 두 곳에 걸치면 주제가 안 좁혀진 것이다.
- `Agent Platform / Guardrails` 는 AI 동작 제한, `Infrastructure / Security` 는
  서버·인증 침해 대응이다. 헷갈리면 "모델이 하는 일을 막는가, 사람이 뚫는 걸 막는가" 로 나눈다.
- `Bioinformatics`, `Design` 은 **의도적으로 뺐다.** 해당 주제 글을 실제로 쓰게 되면
  그때 1단계로 추가한다. 미리 만들지 않는다.
- 만든 프로젝트를 전부 쓰지 않는다. crawl-data, open-data, lang-study 처럼
  **목표와 연결되지 않는 것은 쓰지 않는다.** 글이 늘어날수록 "여러 개 만들어본 사람" 으로
  읽히고, 그건 원하는 인상의 반대다.

```yaml
categories: [Infrastructure, Inference]
```

## 태그 (소문자 kebab-case, 3~5개)

- **고유명사와 기술명만.** `vllm` `mcp` `tool-calling` `fastapi` `dgx-spark` `postgres`
- 감상·분류어 금지. `팁` `정리` `삽질` `개발` 같은 건 태그가 아니다
- 카테고리와 같은 말을 태그로 반복하지 않는다
- 새 태그를 만들기 전에 기존 태그를 먼저 본다 (`/tags/`)

## 제목

1. **한국어, 40자 이내.**
2. **결과나 발견을 담는다.** 무엇을 알아냈는지가 제목에 있어야 한다.
   - ✅ `큰 모델로 바꿨더니 3배 느려졌다`
   - ✅ `개인 비서를 만들려다 GPU 박스를 샀다`
   - ❌ `vLLM 설정 정리` `DGX Spark 사용기` `MCP 알아보기`
3. **구체적인 숫자나 고유명사가 있으면 넣는다.** "3배", "35B", "273GB/s".
4. **맥락과 부제는 제목이 아니라 `description`에 넣는다.** 제목에 콜론·대시로 부제를 붙이지 않는다.
5. **시리즈 표시를 제목에 넣지 않는다.** "1편"은 본문 맨 위 안내줄로 처리한다.
6. **담백하게 서술한다.** "알고 보니 ~였다" 같은 반전 구조를 쓰지 않는다.
   한 번은 자연스럽지만 두 번 겹치면 공식이 보이고, 공식이 보이면 값싸 보인다.
   반전은 제목이 아니라 `description` 에서 풀어도 된다.
7. **앞 글과 문형이 겹치지 않게 한다.** 어미가 연달아 같으면(~였다, ~였다) 하나를 바꾼다.
8. **장애·사고 글은 검색어를 제목에 넣는다.** 같은 문제를 겪는 사람이 검색으로 찾아온다.
   제품명·증상·조치를 그대로 적는다 (예: "셀프호스팅 Gitea 크립토 마이너 감염 대응").

## 파일명

```
_posts/YYYY-MM-DD-english-slug.md
```

- 슬러그는 **소문자 영어 kebab-case.** 그대로 URL(`/posts/english-slug/`)이 된다
- 한글 슬러그 금지. URL이 퍼센트 인코딩되어 공유할 때 깨진다
- **짧게 줄이지 말고 제목을 그대로 영어로 풀어쓴다.** 길어도 된다.
  짧게 만들수록 나중에 겹친다. 슬러그는 한 번 정하면 바꾸지 않는다 (링크가 깨진다)
- 날짜는 발행일. **미래 날짜는 빌드에서 제외된다** (`future: true`가 켜져 있으면 예외)

## 프론트매터 템플릿

```yaml
---
title: 결과를 담은 한 줄
description: 한 문장으로 무엇을 다루는 글인지. 목록과 검색 결과에 이게 나온다.
date: 2026-08-21 20:00:00 +0900
categories: [Agent Platform, Runtime]
tags: [tool-calling, mcp, vllm]
---
```

## 본문 구조

- **`##`부터 시작한다.** `#`는 title이 이미 쓴다
- 한 글은 **주제 하나.** 여러 개면 나눈다
- 표는 비교할 게 실제로 있을 때만. 문단으로 될 걸 표로 만들지 않는다
- 코드 블록에는 언어를 명시한다 (```bash, ```python, ```yaml)
- **명령조(`~해야 한다`, `~하면 안 된다`)를 쓰지 않는다.** 겪은 걸 쓰는 글에 훈수가 섞이면
  누가 한 일인지 흐려지고 튜토리얼 말투가 된다. `찍어봐야 한다` 가 아니라 `찍어봤다` 로 쓴다
- **줄표(—) 금지.** AI 글의 가장 흔한 표식이다. 문장을 끊거나 쉼표·괄호·마침표로 바꾼다.
  제목 뒤 설명이 필요하면 줄바꿈으로 처리한다
- **AI가 쓴 티가 나면 안 된다**: 불릿 남발, "결론적으로", "~할 수 있습니다" 반복,
  모든 문단이 같은 길이, 요약 섹션, 마무리 소감. 사람이 겪은 순서대로 쓴다

## 시리즈

본문 맨 위에 인용문으로 표시한다.

```markdown
> DGX Spark로 LLM을 직접 굴린 기록. 2편.
> 1편 왜 샀나 · **2편. 대역폭** · 3편 오프라인
```

## 용어

- **`온프렘`은 About 의 역량 서술에만 쓴다.** 기업이 클라우드와 대비할 때 쓰는 말이라
  개인 기계를 가리키기엔 크다. 글 본문·제목에서는 `로컬`, `내 서버`, `직접 굴린다` 를 쓴다.
  검색량도 `로컬 LLM` 쪽이 훨씬 많다.
- **같은 용어를 매 글 반복하지 않는다.** 특히 시리즈 안내줄처럼 매 편 들어가는 문장은
  같은 단어가 글 수만큼 늘어난다.

## 보안 (공개 레포다)

- **회사명·소속·학력을 쓰지 않는다.** 지금 방침이다
- 내부 IP, 호스트명, 도메인, 컨테이너명, 사내 서비스명을 쓰지 않는다
- API 키·토큰은 당연히 금지. **한 번 커밋하면 히스토리에 영구히 남는다**
- 내부 시스템은 일반화해서 쓴다: `172.16.0.8` → "다른 기계", `llm-hub` → "게이트웨이"

## 작업 절차

새 글 요청을 받으면:

1. 카테고리를 위 표에서 고른다. 없으면 표를 먼저 고칠지 물어본다
2. `_posts/YYYY-MM-DD-slug.md` 생성
3. 초안을 쓴 뒤 **위 "보안" 항목을 다시 훑는다**
4. 로컬 확인: `docker start blog-preview` → http://localhost:4001
5. **커밋·푸시는 하지 않는다.** 명령어만 알려준다

## 로컬 미리보기

```bash
docker start blog-preview
```

`_config.yml`을 고쳤으면 `docker restart blog-preview`. jekyll serve는 설정 변경을 자동 반영하지 않는다.
